.env
__pycache__/
*.pyc
core/enem_teste.db
core/backups/
core/vision_board/
core/scratch_enunciados_*.txt

# Removidos do histórico do Git em 2026-09-15 (ver ADR-0002 e a decisão de
# publicação em GitHub) -- contêm dado pessoal de uso real (tentativas,
# streak, configuração). Continuam existindo em disco pra uso local; quem
# clonar o repo do zero recria via `python reconstruir_base.py` (ver
# README).
core/enem.db
core/enem.db.bak_pre_import

# Material pessoal de candidatura/marketing (LinkedIn), não documentação
# de projeto -- fica só local, não faz sentido num repositório que
# recrutador vai abrir.
docs/linkedin_projeto.md

# Logs de debug de sessão de teste, não são código.
*.log

# Exports brutos do Claude Design baixados pelo usuário -- redundantes
# com design_handoff_enem_gamificado/ (a versão curada que o código de
# verdade referencia) e um deles carrega o runtime JS do editor +
# fotos pessoais em uploads/ (não algo pra ir pro histórico do git,
# mesmo em repo privado).
Redesign ENEM app gamificado.zip
Redesign ENEM app gamificado (1).zip
Redesign ENEM app gamificado (1)/

# Log pessoal de sessões de estudo (dado bruto, não vai pro repo público)
docs/sessao_estudo_chat.md
docs/estudo_citologia_*.md
docs/proposta_topicos_*.md
core/corpus_analise.db*
docs/plano_50_dias*.md
core/inep_itens/
rows.pkl
docs/registro_estudo.csv
core/inep_ppl/

# Plano pessoal de faculdade (renda, cota, prazos)
docs/plano_faculdade*.md

# Cartilhas do INEP baixadas e textos extraídos (material de terceiros, só consulta local)
docs/redacao_pesquisa/cartilhas/
