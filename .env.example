# Trava simples da API (ver adr/0009-trava-simples-antes-de-autenticacao-real.md).
# Opcional: sem essa variável, a API roda aberta (comportamento de hoje,
# uso pessoal em wifi doméstica). Setando um valor aqui, todo request
# precisa mandar "Authorization: Bearer <o mesmo valor>".
API_AUTH_TOKEN=
